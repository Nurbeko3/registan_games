# Production cloud backend (Render). Public, non-secret — safe to commit.
# Server-only secrets (ADMIN_*, ARENA_AUTHORITY_*) stay in the Vercel dashboard.
NEXT_PUBLIC_API_URL=https://backend-registan-games.onrender.com
